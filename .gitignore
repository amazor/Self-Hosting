# --- IDE / Editor ---
# .cursor/ — tracked so rules/skills follow the repo for remote dev
.idea/
.vscode/
*.swp
*.swo
*~

# --- OS ---
.DS_Store
.DS_Store?
Thumbs.db
ehthumbs.db
Desktop.ini

# --- Secrets & env (example is committed; real .env is not) ---
.env
.env.*
!.env.example
*.pem
*.key
secrets/

# --- Per-stack runtime state (container config/data volumes) ---
docker_compose/*/config/

# --- Claude Code local machine details (example is committed; real file is not) ---
CLAUDE.local.md
!CLAUDE.local.md.example

# --- Claude Code MCP server config (can contain real LAN IPs/hosts) ---
.mcp.json

# --- Claude Code local state (permissions/settings can contain real IPs/usernames; also worktrees, locks) ---
.claude/

# --- Logs & temp ---
*.log
logs/
tmp/
temp/
*.tmp
*.temp
*.bak
*.backup

# --- Docker (when used locally) ---
.docker/
docker-compose.override.yml
# Keep override.example if you use it

# --- Python (if scripts are added) ---
__pycache__/
*.py[cod]
*$py.class
.Python
venv/
.venv/
*.egg-info/
.eggs/

# --- Shell / misc ---
*.pid
.cache/
